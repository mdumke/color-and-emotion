{
  "name": "color-and-emotion",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "parcel build src/index.html --no-source-maps --dist-dir docs --public-url ./",
    "ci": "npm run check && npm run clean && npm run build",
    "clean": "rimraf docs && mkdir docs",
    "start": "parcel src/index.html --open",
    "check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.3"
  }
}
